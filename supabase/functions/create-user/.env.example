# Local testing only (e.g. `supabase functions serve --env-file supabase/functions/create-user/.env`).
# In production, SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are reserved names
# that Supabase injects automatically — you only need to set WEBHOOK_SECRET
# yourself. See the README in this folder for exact deploy steps.

SUPABASE_URL=https://gsfejyokuxccpjghlfzb.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
WEBHOOK_SECRET=your-webhook-shared-secret
